%============================================================================
% LaTeX style file
% Daniel J. Greenhoe
%============================================================================

%--------------------------------------
% lengths
%--------------------------------------
\newlength{\latbot}% bottom space for lattice
\newlength{\latunit}% unlabeled lattice unit
\newlength{\llatunit}% labeled lattice unit
%---------------------------------------
% PS defaults
%---------------------------------------
\psset{
  unit=10mm,
  fillstyle=none,
  %cornersize=relative,
  cornersize=absolute,
  framearc=0.5,
%  linearc=50\psunit,
  linewidth=1.25pt,
  radius=1.25mm,
  dotsep=1pt,
  dotsize=5pt,
  %labelsep=1pt,
  labelsep=1.5mm,
  linecolor=latline,
  gridcolor=graph,
  gridwidth=0.8pt,
  gridlabels=6pt,
  subgriddiv=5,
  subgridwidth=0.2pt,
  %arrowinset=0.6,
  arrowinset=0.2,
  dash=2pt 1pt,
  %arrowlength=1,
  }

\setlength{\latbot}{2mm}% bottom spacing for lattices
\setlength{\latunit}{10mm}% unlabeled lattice unit
\setlength{\llatunit}{7.5mm}% labeled lattice unit
%---------------------------------------
% global parameters
%---------------------------------------
%\newcommand{\dotsizeDiscretePointxyzd}{25pt} % dotsize for discrete point

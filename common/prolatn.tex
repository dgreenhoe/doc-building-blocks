%============================================================================
% Daniel J. Greenhoe
% XeLaTeX file
% projections onto lattices with negations
%============================================================================

%=======================================
\section{Projections}
%=======================================
%---------------------------------------
\begin{definition}
\citetbl{
  \citorppc{nakamura1957}{158}{159}{equation (S)}\\
  \citorpc{sasaki1954}{300}{Def.5.1, cf Foulis 1962}\\
  \citerpg{kalmbach1983}{117}{0123945801}
  }
\label{def:sasaki}
%---------------------------------------
Let $\latL\eqd\latocd$ be an \structe{orthocomplemented lattice} \xref{def:latoc}. % \xref{def:latoc}.
\defbox{\begin{array}{M}
  A function $\sasakin{x}\in\clFxx$ is a \hid{Sasaki projection} on $x\in\setX$ if 
  \\\indentx$ \sasaki{x}{y} \eqd \brp{y \join x^\ocop} \meet x$.
  \\
  The \ope{Sasaki projection}s $\sasakin{x}$ and $\sasakin{y}$ are \hid{permutable} if 
  \\\indentx$\sasakin{x}\circ\sasakin{y}(u)=\sasakin{y}\circ\sasakin{x}(u) \quad\forall u\in\setX$.
\end{array}}
\end{definition}

%---------------------------------------
\begin{proposition}
\label{prop:sasaki_implies}
%---------------------------------------
Let $\sasaki{x}{y}$ be the \ope{Sasaki projection of $y$ onto $x$} \xref{def:sasaki}
in an \structe{orthocomplemented lattice} $\latL\eqd\latbX$.
\propbox{\begin{array}{FMc rcccc C}
  (1). & $x\orel y$                                &\implies&   &     & \sasaki{x}{y} &=    & x  & \forall x,y\in\setX\\ 
  (2). & $y\orel x$                                &\implies& y &\orel& \sasaki{x}{y} &\orel& x  & \forall x,y\in\setX\\
  (3). & $y\orel x$ and $\latL$ is \prope{Boolean} &\implies&   &     & \sasaki{x}{y} &=    & y  & \forall x,y\in\setX
\end{array}}
\end{proposition}
\begin{proof}
\begin{align*}
  x\orel y\implies\sasaki{x}{y} 
    &\eqd \brp{y \join x^\ocop} \meet x 
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&= 1 \meet x 
    && \text{by $x\orel y$ hypothesis and \prefp{prop:latoc_x_orel_y}}
  \\&= x 
    && \text{by property of bounded lattices \xref{prop:latb_prop}}
  \\
  y\orel x\implies\boxed{y}
    &= y\meet x
    && \text{by $y\orel x$ hypothesis}
  \\&\orel (y\join x^\ocop)\meet x
    && \text{by definition of $\join$ \xref{def:join}}
  \\&= \boxed{\sasaki{x}{y}}
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&\orel (y\join x^\ocop)\meet x
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&\orel \boxed{x}
    && \text{by definition of $\meet$ \xref{def:meet}}
  \\
  \text{$y\orel x$ and \prope{Boolean}}\implies \sasaki{x}{y}
    &= (y\join x^\ocop)\meet x
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&= (y\meet x)\join(x^\ocop\meet x)
    && \text{by \prope{distributive} prop. of \structe{Boolean lattice}s \xref{thm:boolean}}
  \\&= (y\meet x)\join0
    && \text{by \prope{non-contradiction} of Boolean lat. \xref{thm:boolean}}
  \\&= (y\meet x)
    && \text{by \prope{boundary} prop. of \structe{bounded lattice}s \xref{prop:latb_prop}}
  \\&= y
    && \text{by $y\orel x$ hypothesis and definition of $\meet$ \xref{def:meet}}
\end{align*}
\end{proof}

%---------------------------------------
\begin{proposition}
\label{prop:sasaki}
%---------------------------------------
Let $\sasaki{x}{y}$ be the \ope{Sasaki projection of $y$ onto $x$} \xref{def:sasaki}
in an \structe{orthocomplemented lattice} $\latbX$.
\propbox{\begin{array}{FrclC}
  (1). & \sasaki{0}{y}                            &=& 0  & \forall y  \in\setX\\
  (2). & \sasaki{x}{0}                            &=& 0  & \forall x  \in\setX\\
  (3). & \sasaki{1}{y}                            &=& 1  & \forall y  \in\setX\\
  (4). & \sasaki{x}{1}                            &=& x  & \forall x  \in\setX\\
  (5). & \sasaki{x}{x^\ocop}                      &=& 0  & \forall x  \in\setX
\end{array}}
\end{proposition}
\begin{proof}
\begin{align*}
  \sasaki{0}{y} 
    &= 0
    && \text{because $0\orel y$ and by \prefp{prop:sasaki_implies}}
  \\
  \sasaki{x}{0} 
    &\eqd \brp{0 \join x^\ocop} \meet x  
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&= x^\ocop \meet x
    && \text{by property of bounded lattices \xref{prop:latb_prop}}
  \\&= 0
    && \text{by definition of \prope{orthocomplemented} \xref{def:latoc}}
  \\
  \sasaki{1}{y} 
    &\eqd \brp{y \join 1^\ocop} \meet 1  
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&= \brp{y \join 0} \meet 1  
    && \text{by \prope{boundary condition \xref{thm:latn_ortho}}}
  \\&= y \meet 1
    && \text{by property of bounded lattices \xref{prop:latb_prop}}
  \\&= 1
    && \text{by property of bounded lattices \xref{prop:latb_prop}}
  \\
  \sasaki{x}{1} 
    &= x
    && \text{because $x\orel 1$ and by \prefp{prop:sasaki_implies}}
  \\
  \sasaki{x}{x^\ocop} 
    &\eqd \brp{x^\ocop \join x^\ocop} \meet x 
    && \text{by definition of \ope{Sasaki projection} \xref{def:sasaki}}
  \\&= x^\ocop \meet x 
    && \text{by \prope{idempotency} of lattices \xref{thm:lattice}}
  \\&= 0 
    && \text{by \prope{non-contradiction} property of \structe{orthocomplemented lattice} \xref{def:latoc}}
\end{align*}
\end{proof}                                                     


%---------------------------------------
\begin{example}
%---------------------------------------
\exboxt{
Here are some examples of projections in the \structe{$O_6$ lattice} %\xref{def:o6} 
onto the element $x$:
\\\indentx$\begin{array}{*{6}{lc}lD}%
    \sasaki{p}{q}       &\eqd& (q       &\join& p^\ocop) &\meet& p &=& p^\ocop &\meet& p &=& \bzero & (because $p \orthog q$)\\
    \sasaki{p}{p^\ocop} &\eqd& (p^\ocop &\join& p^\ocop) &\meet& p &=& p^\ocop &\meet& p &=& \bzero & (because $p \orthog p^\ocop$)\\
    \sasaki{p}{q^\ocop} &\eqd& (q^\ocop &\join& p^\ocop) &\meet& p &=& \bid    &\meet& p &=& p      & (because $p \le q^\ocop$)\\
    \sasaki{q^\ocop}{p} &\eqd& (p       &\join& q      ) &\meet& q^\ocop &=& \bid    &\meet& q^\ocop &=& q^\ocop  & (because $q^\ocop\orel1$)\\
    \sasaki{p}{\bid}    &\eqd& (\bid    &\join& p^\ocop) &\meet& p &=& \bid    &\meet& p &=& p      & (because $p \le \bid$)\\
    \sasaki{p}{\bzero}  &\eqd& (\bzero  &\join& p^\ocop) &\meet& p &=& p^\ocop &\meet& p &=& \bzero & (because $p \orthog \bzero$)
  \end{array}$\hfill%
  \begin{tabular}{c}%
    \psset{unit=6mm}%
    \gsize%
    \input{../common/math/graphics/lat/lat6_o6_pq.tex}%
  \end{tabular}%
  }
\end{example}

%---------------------------------------
\begin{example}
%---------------------------------------
\exboxt{
Here are some examples of projections in lattice 5 of \prefpp{ex:latoc}:
\\\indentx$\begin{array}{*{6}{lc}lD}
    \sasaki{x}{p}       &\eqd& (p       &\join& x^\ocop) &\meet& x &=& \bid    &\meet& x &=& x      & \\
    \sasaki{x}{y}       &\eqd& (y       &\join& x^\ocop) &\meet& x &=& x^\ocop &\meet& x &=& \bzero & (because $x \orthog y$)\\
    \sasaki{x}{z}       &\eqd& (z       &\join& x^\ocop) &\meet& x &=& x^\ocop &\meet& x &=& \bzero & (because $x \orthog z$)\\
    \sasaki{x}{p^\ocop} &\eqd& (p^\ocop &\join& x^\ocop) &\meet& x &=& p^\ocop &\meet& x &=& \bzero & \\
    \sasaki{x}{x^\ocop} &\eqd& (x^\ocop &\join& x^\ocop) &\meet& x &=& x^\ocop &\meet& x &=& \bzero & (because $x \orthog x^\ocop$)\\
    \sasaki{x}{y^\ocop} &\eqd& (y^\ocop &\join& x^\ocop) &\meet& x &=& \bid    &\meet& x &=& x      & (because $x \le y^\ocop$)\\
    \sasaki{x}{z^\ocop} &\eqd& (z^\ocop &\join& x^\ocop) &\meet& x &=& \bid    &\meet& x &=& x      & (because $x \le z^\ocop$)\\
    \sasaki{x}{\bid}    &\eqd& (\bid    &\join& x^\ocop) &\meet& x &=& \bid    &\meet& x &=& x      & (because $x \le \bid$)\\
    \sasaki{x}{\bzero}  &\eqd& (\bzero  &\join& x^\ocop) &\meet& x &=& x^\ocop &\meet& x &=& \bzero & (because $x \orthog \bzero$)
  \end{array}$
  }
\end{example}


%---------------------------------------
\begin{example}
\label{ex:latoc_r3_projection}
%---------------------------------------
%\exbox{\begin{tabular}{m{\tw-73mm}|m{54mm}}
\exbox{\begin{tabular}{lm{54mm}}
  \parbox{\tw-73mm}{%
  Let $\R^3$ be the \structe{3-dimensional Euclidean space} \xref{ex:latoc_r3}
  with subspaces $\spZ$ and $\spV$.
  Then the projection operator $P_{\spZ^\ocop}$ onto $\spZ^\ocop$ is a \ope{sasaki projection} $\sasakin{\spZ^\ocop}$.
  In particular
    \\\indentx$\begin{array}{rcl}
      \opP_{\spZ^\ocop}\spV &\eqd& \sasaki{\spZ^\ocop}{\spV} 
                          \\&\eqd& \brp{\spV + \spZ^{\ocop\ocop}}\seti\spZ^\ocop
                          \\&=&    \brp{\spV + \spZ}\seti\spZ^\ocop
    \end{array}$\\
  as illustrated to the right.}
  &\centering\psset{unit=8mm}\footnotesize\input{../common/math/graphics/r3_projection.tex}
  \end{tabular}}
\end{example}


%%---------------------------------------
%\begin{lemma}
%\label{lem:numDL}
%%---------------------------------------
%Let $\latL_2^\xN$ be a \structe{Boolean lattice} of order $\xN$.
%\\\lemboxt{
%  The number of pairwise orthogonal elements in $\latL_2^\xN$ is
%  \\\indentx
%    $\brbl{\begin{array}{lD}
%      0                        & for $\xN<2$\\
%      1                        & for $\xN=2$\\
%      3                        & for $\xN=3$\\
%      \xN + \bcoef{\xN}{\xN-2} & for $\xN>3$
%    \end{array}}$
%  }
%\end{lemma}
%\begin{proof}
%
%\end{proof}

